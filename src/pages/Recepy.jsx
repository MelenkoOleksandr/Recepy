// Page for the recepy
// It must contain the list of ingredients and the steps to follow
// Need to show insufficient products depending on the fridge

const Recepy = () => {
  return <div>Recepy</div>;
};

export default Recepy;
